import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  base: {
    height: 40,
    marginBottom: 16,
    paddingHorizontal: 8,
    borderWidth: 1,
    borderRadius: 5,
  },
})

export default styles
